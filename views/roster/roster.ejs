<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roster Details</title>
    <link rel="stylesheet" href="../../public/css/styles.css">
</head>

<body>
    <h1>
        <% if (roster) { %>
            <%= roster.name %>
                <% } else { %>
                    Your NBA Roster
                    <% } %>

    </h1>

    <% if (message) { %>
        <p>
            <%= message %>
        </p>
        <% } else { %>
            <% if (players && players.length> 0) { %>
                <h2>Roster Details</h2>
                <form action="/roster?_method=DELETE" method="POST">
                    <button type="submit">Delete <%= roster.name %></button>
                </form>
                <form action="/roster?_method=PUT" method="POST">
                    <p>Type new name</p>
                    <input type="text" name="rosterName" required>
                    <button type="submit">Update <%= roster.name %></button>
                </form>
                <p>Total Points: <%= roster.totalPoints %>
                </p>
                <p>Total Rebounds: <%= roster.totalRebounds %>
                </p>
                <p>Total Assists: <%= roster.totalAssists %>
                </p>
                <a href="/players">Search Player</a>
                <h2>Players</h2>
                <div>
                    <% players.forEach(function(player) { %>
                        <div>
                            <h3>
                                <%= player.first_name %>
                                    <%= player.last_name %>
                            </h3>
                            <p>Position: <%= player.position %>
                            </p>
                            <p>Height: <%= player.height %>
                            </p>
                            <p>Jersey Number: <%= player.jersey_number %>
                            </p>
                        </div>
                        <% }); %>
                </div>
                <% } else { %>
                    <p>No players found in this roster.</p>
                    <% } %>
                        <% } %>
</body>

</html>